[pytest]
# asyncio_mode=auto so an `async def test_` runs without a decorator on every
# one of them. The suite is almost entirely async because the code under test
# is.
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
